# 系统架构与运行流程

> 当前实现说明，最后核对于 2026-09-26。

## 1. 总体架构

```text
前端
  │  REST / SSE
  ▼
FastAPI API
  │  鉴权、会话/分支解析、请求幂等、Trace Run
  ▼
ReAct Runtime
  └─ react        ──> 受控 ReAct Graph
                         │
                         ├─ Context Builder
                         ├─ Controller
                         ├─ Tool Gateway
                         │    ├─ knowledge_search
                         │    └─ web_search
                         ├─ Evidence Ledger
                         ├─ Verification
                         └─ Memory Update Plan

持久化：MySQL（业务/记忆/Trace）+ Chroma（知识库 Dense 向量）
       + SQLite Checkpointer（ReAct State，单实例阶段）
```

入口关系：

- API：`backend/api/v1/endpoints/rag.py`
- 在线入口：`backend/agent/runtime.py`
- 新图：`backend/agent/graph.py`
- 旧图：`backend/graph/graph.py`

## 2. 唯一在线运行模式

新 ReAct Graph 直接承接所有在线请求，受总 deadline、迭代预算和工具预算约束。`get_runtime_mode()` 固定返回 `react`，旧环境中的 `legacy`、`react_shadow` 或非法值都不会使 API 回到旧图。

`backend/graph/` 当前仅供历史评测脚本迁移参考，不被 `backend/agent/runtime.py` 导入。

## 3. 新 ReAct Graph

```text
hydrate_context
      │
      ▼
    decide ── final/refusal/clarification ──> verify
      │                                         │
      │ tool_calls                              │ 验证失败且仍有预算
      ▼                                         └──────────> decide
     act
      │
      ▼
   observe
      │
      └────────────────────────────────────────> decide

verify ── publishable ──> propose_memory_update ──> END
```

节点职责：

| 节点 | 职责 |
| --- | --- |
| `hydrate_context` | 校验独立 Session，加载画像、会话记忆和关键词命中的长期记忆，创建 Working Memory v2，暴露允许使用的工具 |
| `decide` | 每轮重新组装上下文，由 Controller 输出结构化 `AgentDecision` |
| `act` | 经 Tool Gateway 授权、校验、幂等和重试后执行工具 |
| `observe` | 规范化 Observation，更新 Evidence Ledger 和 Working Memory |
| `verify` | 检查答案是否可发布、是否有证据支持、引用是否有效 |
| `propose_memory_update` | 答案成功后生成记忆更新计划 |

## 4. 决策契约

Controller 不返回任意文本动作，而是返回 `AgentDecision`：

```text
type:
  - tool_calls
  - final_answer
  - refusal
  - clarification

tool_calls[]:
  - tool_call_id
  - tool_name
  - arguments
  - purpose
  - target_slots

answer
cited_evidence_ids[]
reason_summary
unresolved_slots[]
confidence
```

`reason_summary` 是简短、可审计的决策摘要，不保存或展示模型内部思维链。事实性结论只能引用 Evidence Ledger 中的 `E#`，长期记忆 `MEM#` 只能帮助理解用户。

## 5. 停止与防失控机制

运行时同时使用多种边界：

- 最大迭代数；
- 最大工具调用数；
- 知识库与 Web 单独调用预算；
- 连续两轮无新增 Evidence；
- 总运行 deadline；
- Controller 错误；
- Verification 最终失败；
- 模型主动输出拒答、澄清或最终答案。

默认预算由 `RuntimeBudget` 和环境变量共同控制。工具错误会变成 Observation 返回图中，不让模型绕过策略层直接执行服务。

## 6. Evidence Ledger

工具输出不会直接散落在 Prompt 中，而是进入统一账本：

```text
EvidenceLedger
  entries: E1, E2, ...
  slot_coverage:
    slot-1: supported / partial / missing / conflict
  conflicts: []
  next_index
```

它负责：

- 给证据分配稳定的运行内编号；
- 去重和记录来源；
- 关联答案槽位；
- 记录冲突；
- 为最终引用构建可追溯来源；
- 为 Verification 提供允许引用的事实集合。

## 7. 会话与数据隔离

所有请求都必须携带服务端解析后的 `user_id`、`session_id` 和 `request_id`，可选 `kb_id`。`branch_id` 只为旧接口兼容保留；新分支是独立 Session。隔离在多层实施：

1. API 校验会话归属和请求范围；
2. 普通会话不隐式加载分支；分叉时复制截止点消息和安全摘要到新 Session；
3. Tool Gateway 删除模型参数中的身份字段；
4. Tool Policy 使用服务端上下文重新校验 Session 和 KB ACL；
5. Checkpoint `thread_id` 包含 User 与 Request，工具幂等键包含 Run、Tool Call、User、Session；
6. Trace API 按当前用户过滤；
7. 分叉后的 Session 只读取自己的消息和 Session Summary，与源会话独立。

模型不能通过工具参数伪造 `user_id`、`session_id`、`kb_id` 或 `branch_id`。

## 8. 当前差距

- 旧评测脚本仍有直接调用 `backend.graph.graph` 的历史路径；
- SQLite Checkpointer 适合单实例，多实例部署仍需迁移到 Postgres/MySQL Saver；
- Decision 没有独立持久化表；
- 原始大体积 Tool 输出尚未通过 `output_ref` 外置；
- 前端需要把“分支”交互统一呈现为跳转到独立 Session，并补充同 request_id 恢复入口；
- 生产认证、限流、密钥和网络边界仍需加固。

Trace 与恢复的准确现状见《03-状态、追踪与失败恢复》，工具和检索细节见《04-工具系统与检索》。
